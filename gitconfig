[user]
	name = Koen Handekyn
	email = koen.handekyn@quine.be
[color]
	ui = auto
[core]
	excludesfile = /Users/koenhandekyn/.gitignore
	pager = delta
[interactive]
	diffFilter = delta --color-only
[mergetool]
	keepBackup = false
	prompt = false
[push]
	default = simple
	autoSetupRemote = true
[pull]
	ff = only
[submodule]
	recurse = true
[delta]
	line-numbers = true
	syntax-theme = gruvbox-dark
	file-style = bold yellow
	file-decoration-style = yellow ul ol
	hunk-header-style = italic yellow
	hunk-header-decoration-style = none
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
